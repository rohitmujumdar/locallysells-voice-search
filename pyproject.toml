[project]
name = "locallysells-voice-search"
version = "0.1.0"
description = "Vapi voice-search tool handler for LocallySells"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
]
